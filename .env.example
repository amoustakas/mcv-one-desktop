# MCV One Desktop — Environment Variables
# Copy to .env and fill in your keys

# Claude API (server-side, used by /api/chat)
ANTHROPIC_API_KEY=

# Gemini API (server-side, used by /api/gemini)
GOOGLE_AI_KEY=

# Client-side keys (prefixed with VITE_)
VITE_APP_ENV=development
