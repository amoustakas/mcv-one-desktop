// Thin shim — canonical implementation lives in @mcv/kits-sdk/registry-client.
// The SDK defaults to '/api/kit-registry' which matches MCV Desktop's mount.
export * from '@mcv/kits-sdk/registry-client';
