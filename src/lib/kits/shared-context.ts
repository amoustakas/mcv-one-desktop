// Thin shim — canonical implementation lives in @mcv/kits-sdk/shared-context.
// The SDK defaults to '/api/kit-context' which matches MCV Desktop's mount.
export * from '@mcv/kits-sdk/shared-context';
