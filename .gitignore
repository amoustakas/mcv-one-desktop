# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment
.env
.env.local
.vercel

.vercel
.env*.local

# Claude Code scratch state — don't commit
.claude/scheduled_tasks.lock
.claude/sessions/
.claude/worktrees/
.superpowers/

# Parallel-session scratch + debug artifacts — each produces thousands of files
# that VS Code would otherwise index as "untracked" and balloon the change count.
# Content is still on disk; just not in git or VS Code's source control view.
.tmp.driveupload/
.playwright-mcp/

# UI walkthrough screenshots (use docs/ or per-PR attachments instead)
local-state-*.png

# Stray storyboard / preview PNGs at repo root — land them in
# .docs/storyboards/<sequence-name>/ so they live with context.
/mcv-*.png

# Scratch / research-sample workspace — keep locally, don't pollute git.
# If a sample graduates into real work, lift it to the proper path first.
.sandbox/

# One-off backup files from AI/assistant workflows
*.gemini.bak.ts
*.gemini.bak.tsx
